# Lokale Secrets (nicht committen)

Dieses Verzeichnis ist in `.gitignore` (außer `*.example`).

- `github-issues.token` — optional, nur für lokale Debug-Builds (Issues-only-PAT)
- Siehe `docs/ci/github-issues-token.md`

**App-Store-iOS-Builds** betten kein Token ein (`Scripts/ios-archive-appstore.sh`).
