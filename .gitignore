.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Superpowers SDD scratch
.superpowers/

# Local git worktrees (agent isolation)
.worktrees/

# Browser HARs (often contain JWTs / PII) — use redacted docs/fixtures instead
HAR/
*.har

# LLVM coverage profiles (local test runs)
*.profraw

# On-demand UI-Review-Tour (PNG/AX, nicht committen)
/DerivedData/ui-review/

# iOS / XcodeGen / SweetPad (generated)
*.xcodeproj/
*.xcworkspace/
!default.xcworkspace
buildServer.json
.sweetpad/
Apps/ReiseniOS/xcuserdata/

# Lokale Provisioning-Profile (Team-Signing, nicht committen)
.signing/

# Local secrets (never commit)
Secrets/*
!Secrets/*.example
!Secrets/README.md

# XOR-Payload zur Compile-Zeit (SSOT: GitHubIssueToken.generated.swift.stub)
Sources/ReisenAppCore/GitHubIssues/GitHubIssueToken.generated.swift

# Python
__pycache__/
*.py[cod]
